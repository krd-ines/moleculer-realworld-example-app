apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: metrics-service
  template:
    metadata:
      labels:
        app: metrics-service
    spec:
      containers:
        - name: moleculer-app
          image: YOUR_DOCKERHUB_USERNAME/moleculer-conduit:latest
          imagePullPolicy: Always
          env:
            - name: SERVICES
              value: "services/metrics.service.js"
            - name: TRANSPORTER
              value: "nats://nats-service:4222"
            - name: MONGO_URI
              value: "mongodb://REPLACE_WITH_DB_PRIVATE_IP:27017/conduit"