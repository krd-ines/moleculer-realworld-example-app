apiVersion: apps/v1
kind: Deployment
metadata:
  name: comments-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: comments-service
  template:
    metadata:
      labels:
        app: comments-service
    spec:
      containers:
        - name: moleculer-app
          image: YOUR_DOCKERHUB_USERNAME/moleculer-conduit:latest
          imagePullPolicy: Always
          env:
            - name: SERVICES
              value: "services/comments.service.js"
            - name: TRANSPORTER
              value: "nats://nats-service:4222"
            - name: MONGO_URI
              value: "mongodb://REPLACE_WITH_DB_PRIVATE_IP:27017/conduit"