apiVersion: apps/v1
kind: Deployment
metadata:
  name: follows-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: follows-service
  template:
    metadata:
      labels:
        app: follows-service
    spec:
      containers:
        - name: moleculer-app
          image: mariaboukhelfa2025/moleculer-conduit:latest
          imagePullPolicy: Always
          env:
            - name: SERVICES
              value: "services/follows.service.js"
          envFrom:
            - configMapRef:
                name: moleculer-env