apiVersion: apps/v1
kind: Deployment
metadata:
  name: favorites-service
spec:
  replicas: 2 # You can scale this because you have 6 worker nodes!
  selector:
    matchLabels:
      app: favorites-service
  template:
    metadata:
      labels:
        app: favorites-service
    spec:
      containers:
      - name: moleculer-app
        image: moleculer-registry:5000/moleculer-conduit:local
        imagePullPolicy: Always
        env:
        - name: SERVICES
          value: "services/favorites.service.js"          # Only starts the Users service
        - name: TRANSPORTER
          value: "nats://nats-service:4222"
        - name: MONGO_URI
          value: "mongodb://mongo-service:27017/conduit"